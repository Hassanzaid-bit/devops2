pipeline {
    agent any
    environment {
        SSH_KEY = credentials('SSH_KEY')
    }
    stages {
        stage('Example') {
            steps {
                echo SSH_KEY
                // sh '''
                //     # Use the SSH key for authentication in your commands
                //     ssh-agent bash -c "ssh-add ${SSH_KEY} && your-command-here"
                // '''
            }
        }
    }
}
